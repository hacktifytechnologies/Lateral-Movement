# T1021.002 – SMB PsExec-style | Assessment

## MCQ 1
PsExec copies its service binary to which Windows admin share by default?
A) C$  B) IPC$  C) ADMIN$ (maps to C:\Windows\)  ✅  D) PRINT$

## MCQ 2
`sc \\remotehost create SvcName binpath= "cmd.exe /c payload.bat"` communicates with:
A) RPC over TCP 135  B) The remote Service Control Manager (SCM) via SMB/port 445  ✅
C) WMI port 5985  D) Remote Registry port 445

## MCQ 3
PsExec-style techniques generate which Windows event on the TARGET host?
A) Event ID 4688 only  B) Event ID 7045 (service install) + 4624 Type 3 (network logon)  ✅
C) Event ID 4776 only  D) No events on the target

## Fill 1
ADMIN$ share maps to which Windows directory:
**Answer:** `C:\Windows`

## Fill 2
Service type used by PsExec on the remote host (runs as which user):
**Answer:** `SYSTEM` (LocalSystem)
