# T1550.002 – Pass the Hash | Assessment

## MCQ 1
Pass-the-Hash works against NTLM authentication because:
A) NTLM has a known backdoor  B) Windows NTLM auth uses the hash as a proof-of-identity directly; cracking to plaintext is unnecessary  ✅
C) The hash is always the same as the password  D) NTLM is disabled on modern Windows

## MCQ 2
Impacket's `wmiexec.py -hashes :NTLM_HASH` uses which format for hashes?
A) `LM:NTLM`  ✅ (LM part can be empty: `:NTLM_HASH`)  B) `NTLM:LM`  C) Only the full LM hash  D) SHA256 only

## MCQ 3
`mimikatz sekurlsa::pth /user: /ntlm:` spawns a new process that:
A) Cracks the NTLM hash before authenticating  B) Runs the specified process with an injected NTLM token, allowing authenticated connections to remote systems  ✅
C) Creates a new Windows account  D) Modifies the SAM database

## Fill 1
Windows Logon Type number for network authentication (used by PtH):
**Answer:** `3`

## Fill 2
Windows security feature that prevents dumping NTLM hashes from LSASS:
**Answer:** `Credential Guard` (Windows Defender Credential Guard)
