# T1021.002 – SMB Brute Force | Assessment

## MCQ 1
CrackMapExec is particularly useful for SMB lateral movement because:
A) It cracks NTLM hashes  B) It performs credential validation AND command execution across multiple SMB targets simultaneously  ✅
C) It generates Kerberos tickets  D) It requires no authentication

## MCQ 2
SMB brute force attempts generate which Windows Security event?
A) Event ID 4624 (success) and 4625 (failure)  ✅  B) Event ID 7045 only  C) Event ID 4688 only  D) No events

## MCQ 3
Account lockout policy mitigates SMB brute force by:
A) Blocking SMB traffic  B) Locking the account after a defined number of failed attempts  ✅
C) Disabling NTLM  D) Requiring Kerberos for SMB

## Fill 1
`net use` command to disconnect a mapped drive:
**Answer:** `net use Z: /delete`

## Fill 2
CrackMapExec flag to run a command on successfully authenticated hosts:
**Answer:** `-x "command"` (for CMD) or `-X "command"` (for PowerShell)
